# v1.2.0 Changelog

> 文档站版本：[docs/releases/v1.2.0.md](docs/releases/v1.2.0.md)。开发过程与各子项目的设计、执行记录见 `docs/superpowers/`。

> v1.1 让摊盒「能用」，v1.2 让它**记得清账**。
> 这一版重写了账本：每一件货、每一分钱都有来处和去处。套装优惠、退货、赠送报废、收摊盘点、寄售分账都建立在新账本上。

> [!WARNING]
> **升级前必读：旧的展会和订单不会迁移**
>
> 商品库会自动带过来，但 v1.1 的**展会、订单、库存不会出现在 v1.2 里**。它们完整保留在备份文件中，
> 升级后第一次登录管理后台时可以一键导出成 Excel。**请不要在展会进行中升级。**
> 详见[从 v1.1 升级到 v1.2](https://boothkernel.secret-sealing.club/guide/upgrade-v1.2)。

## 主要更新

### 📒 新账本：货和钱都记双边

以前的库存只是一个数字，卖了就减、取消就加，算错了查不出原因。
现在每次下单、收款、退货、赠送、盘点都是一笔**借贷必平**的分录：货从哪个仓到哪个仓、钱从哪个渠道到哪个社团，一笔不漏。

- 收款时要选**收款渠道**（微信 / 支付宝 / 现金 / 自定义），收摊时按渠道对账
- 「完成订单」仍然**只是记账**，不代表系统确认了到账，这一点没有变

### 🎁 套装与优惠

- 在展会里配**套装**：「明信片任选 3 张 20」（可以拿同款）或「新刊 + 立牌 一起 50」（各 1 件凑齐）
- 顾客购物车**自动求出最省的组合**，并写明「已应用：XX 套装 −¥10」
- 摊主收款时可以**逐个拆掉套装**、直接改**实收**（抹零或加价），差额记成手工折让，不影响代卖社团的分成
- 配套装时实时试算「顾客最多 / 最少能怎么拿」，避免配错
- 取代了 v1.1 的「负价格商品当折扣」

详见[套装与优惠](https://boothkernel.secret-sealing.club/guide/lots)。

### 🧾 收摊与结算

- 摊主端新增**收摊向导**：清点订单 → 盘点 → 带回 → 结算，中途退出不丢进度
- **结算单**：每个社团的货去了哪、净额多少、我应转给谁多少，一张表看清；可导出四个 sheet 的 Excel
- **寄售分账**：商品按「社团」归属，代卖别人的货自动单独结算
- **退货**（逐行、可部分退、可换退款渠道）、**赠送 / 报废**登记、**垫付**、**结算调整**、**收摊清点**
- 结算后账本冻结，只有垫付、结算调整、收摊清点三项还能回家补

详见[收摊与结算](https://boothkernel.secret-sealing.club/guide/closing)。

### ▦ 条码扫描

- 点单页用摄像头**连续扫条码加购**，商业条码（JAN / ISBN）和自己贴的编号标签都能扫
- 支持 USB / 蓝牙**扫码枪**，插上即用
- 商品表单可以「扫码填入」条码
- 全程离线，不依赖任何网络服务

详见[条码扫描](https://boothkernel.secret-sealing.club/guide/barcode-scan)。

### 🧭 重新设计的管理后台和摊主端

- **展会工作台**：进入一场展会，顶部是「筹备 → 进行中 → 已结算」状态条，标签页按「展前 / 现场 / 收摊」分组
- **展前准备**：「从商品库选」批量上架；「**从上一场导入**」一次搬过商品、售价和套装
- 侧栏固定为 展会 / 商品库 / 社团 / 设置 / 使用教程；原「控制台」「关于」并入「设置」
- **摊主端**改为 订单 / 库存 / 收摊 三个 tab，手机上是底部 tab 栏；切 tab 不影响新单提醒
- 摊主端「**去点单**」、点单页「**回摊主端**」，平板代点单来回一键切换
- 摊主可以直接看这场的结算单
- 订单列表显示已退金额和逐行已退数量
- 全站统一的组件和配色，平板上的点单页和手机上的摊主端都重新排版

## 修复与改进

- 修复 Windows 缺少 VC++ 运行库时**启动即闪退**的问题：安装包自带运行库，AI 模型加载失败时只关闭拍照识别，不再影响整个 App
- Android 的 AI 识别「自动」模式改为走 CPU（实测 NNAPI 对这些模型反而更慢，仍可在设置里手动选）
- 修复开摊时没有待处理订单、**第一张新单不响提示音**的问题
- 修复局域网浏览器里导出 Excel 失败的问题
- 修复拍照识别的一批问题：增量建索引丢图、重建失败却显示「完成」、导入商品包后不建索引、相册长图被拉伸
- 识别不可用时，点单页的「拍照识别」入口置灰，不再点进去才报错
- 修复摄像头关闭后指示灯仍亮（摄像头未释放）的问题
- `.boothpack` 导入加固：拒绝写到数据目录以外的条目
- 管理后台常驻提醒修改出厂默认密码（`admin123` / `vendor123`）
- 全站界面文字统一为中文（原来部分输入框占位是英文「Please Input」）
- 每次升级前自动给数据库拍快照，升级失败自动还原

## 升级须知

- **Windows**：v1.1.1 用户在「设置 → 关于与更新」一键更新；v1.1.0 用户需手动下载安装包
- **Android**：下载新 APK 覆盖安装，**不要先卸载**（卸载会清空数据）
- 首次启动后请看[从 v1.1 升级到 v1.2](https://boothkernel.secret-sealing.club/guide/upgrade-v1.2)，导出旧数据、检查社团归属
- 局域网访问仍然使用 5141 端口（HTTPS），证书和二维码的用法不变
