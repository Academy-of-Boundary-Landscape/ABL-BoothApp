# ③b 形状清理清单

阶段 2 各模块迁移时发现的「想改但不许改」的 JSON 形状问题（spec K3）。Task 8 整理、Task 10 统一处理。

## 处置（Task 8，2026-09-25）

77 条按性质分四类。判据是 spec §6「用户可见行为零变化」：③b 只收契约，不改行为。

| 类 | 条目 | 处置 | 理由 |
|---|---|---|---|
| **A 错误体不统一**：纯文本 / `{"ok":false,...}` / 英文或内部错误原文 | closing 以外几乎每个 v1.1 模块都有（stats、event、admin、master_product、sync、legacy、vision、auth 的 DebugJson） | **不改**，文档已如实标成 `text/plain` 或对应形状 | 现在前端读不到 `error` 字段，显示的是**调用方的中文兜底文案**；改成 `{"error":"Database Error"}` 反而让用户看到英文。要改就得同时重写每条文案，属于 ④ |
| **B 吞错误**：`unwrap_or_default()` / `unwrap_or(None)` / `let _ =` | stats、event、admin、master_product、sync、legacy、vision、auth、order 分组、closing 账面缺失 | **不改** | 改了就会出现新的 500 或 404，是行为变化；需要配合前端的错误态设计，留给后续 |
| **C 形状别扭或语义重载** | `OrderRow` flatten 到顶层；`OrderItemResponse.product_id` 实为 event_product_id；`journal_id: null` 语义重载；settle 恒返回常量；删除返回 200+message 而非 204；lot 的哨兵 `0`；refund 多余字段；`default_price`/legacy 金额是 f64 元 | **不改**，记为后续候选 | 牵动面大、收益在以后；`default_price` 改分牵涉 `.boothpack` 跨版本兼容 |
| **D 值域固定却只写成 string** | 收摊 `status`（进行中/已结算）；以及同类的展会状态、订单状态、退货去向、调整方向 | **Task 10 做** | 只在 schema 上声明枚举（`#[schema(value_type = …)]` 或文档用 enum），**线上 JSON 一个字节不变**，TS 侧得到字面量联合类型，拼错状态字符串在编译期报错 |

Task 10 之外的条目保留在下表，作为 ④ 与后续版本的输入。

| 模块 | 路由 | 现状 | 建议 | 理由 | 前端消费方 |
|---|---|---|---|---|---|
| closing | GET /events/{id}/closing | `status` 是中文自由字符串（进行中/已结算） | 改成稳定枚举值（如 `open`/`settled`） | 前端用 `s.status === '已结算'` 比较原文，文案一改契约就断；字符串没有类型约束 | ClosingWizard.vue（`step` computed） |
| closing | POST /events/{id}/closing/stocktake | `journal_id: Option<i64>`，`null` 兼表「零差异、没写 journal」 | 加 `wrote_journal: bool`，或让 `journal_id` 只在真写了时出现 | `null` 同时是「没有 id」和「没写 journal」两种意思，消费方只能靠 `diffs` 反推 | 无（closingStore.stocktake 丢弃响应体） |
| closing | POST /events/{id}/closing/takeback | `journal_id: Option<i64>`，空摊 no-op 时为 `null` | 同 stocktake，加 `wrote_journal: bool` | 同 stocktake：`null` 语义重载 | 无（closingStore.takeback 丢弃响应体） |
| closing | POST /events/{id}/closing/settle | 成功返回 `200 {"status": "已结算"}`，该字段恒为常量 | 改成 `204 No Content` | 前端在成功后重新拉 `GET /closing`，常量 body 不携带信息 | 无（closingStore.settle 丢弃响应体） |
| closing | POST /events/{id}/closing/stocktake | `book.get(&event_product_id).unwrap_or(&0)` 把「账面查不到该商品」当余额 0 | 显式判缺失并返回 400 | 目前靠后面 `event_products` 的名字查询兜底，但默认 0 会掩盖 `book_balances` 漏项；新调用路径可能绕过 | 无 |

| inventory | `POST /events/{event_id}/scraps` | 请求体沿用 `LogRequest`，其中的 `vendor_pays` 被静默置为 false（不报错） | 报废单独定义一个不含 `vendor_pays` 的请求类型 | 该字段对本接口无意义，但 OpenAPI 把它列成可填，客户端会误以为生效 | `frontend/src/components/vendor/InventoryLogModal.vue`（报废时本就不发该字段） |
| lot | GET /events/{event_id}/lots | 候选被级联删空后，LEFT JOIN 的 NULL 被 `owner.unwrap_or(0)` / `owner_name.unwrap_or_else(...)` 吞掉，`owner_society_id` 输出哨兵 `0`、`owner_society_name` 输出 `"（候选已被删除）"` | 两个字段改 `Option<...>`，无候选时输出 `null`，显示文案交给前端 | 库里不存在 0 号社团；哨兵 `0` 让「没有货主」和「货主 id 恰好是 0」在 JSON 上无法区分，且字符串文案是后端替前端做的显示决定 | frontend/src/views/AdminEventLots.vue（`lot.owner_society_name` 表格列） |
| order | GET `/events/{event_id}/orders` | 内存分组后 `items_map.remove(&oid).unwrap_or_default()` / `lots_map.remove(&oid).unwrap_or_default()` 把「本应存在的 items/lots 行不在 map 里」静默变成空数组 | 分组后断言没有剩余的未消费 map 项，或在缺失时落日志 | 响应形状本身正确，但会掩盖 JOIN/分组键漂移导致的账目错位（订单显示成 0 件） | `frontend/src/stores/orderStore.js`、`frontend/src/views/AdminEventOrders.vue` |
| order | POST/GET/PUT `/events/{event_id}/orders` | `OrderItemResponse.product_id` 实际存的是 `event_product_id` | 改名为 `event_product_id` | 字段名与值指向不同实体，容易被当成全局 `master_products.id` 使用 | 这五个消费方目前都没读它；改名仍是形状变更 |
| order | POST/GET/PUT `/events/{event_id}/orders` | 响应把 `OrderRow`（DB 行）`#[serde(flatten)]` 摊平到顶层，与 `items`/`lots` 同级 | 收进 `order: { ... }` 子对象 | API 形状与 `SELECT *` 的列强耦合，`OrderRow` 加一列就自动变成公开契约 | 全部五个消费方都读顶层 `final_amount`/`gross_amount`/`solved_amount`/`timestamp`/`channel` |
| refund | `POST /events/{event_id}/orders/{order_id}/refunds` | 201 响应带 `allocated_total`、`paid_total` 两个内部账务合计 | 契约里可只留 `journal_id` 与 `refund_amount` | 前端只消费 `refund_amount`，另两个是内部量，固化进契约会让实现细节外泄 | `frontend/src/components/vendor/RefundModal.vue`（只读 `data.refund_amount`） |
| refund | `GET /events/{event_id}/orders/{order_id}/refunds` | `RefundableLine` 的 `event_product_id`、`product_code`、`refunded_qty`、`remaining_allocated`，以及 `RefundHistoryRow` 的 `order_line_id`、`product_code` 前端从不读取 | 可考虑精简未消费字段 | 响应面比消费方需要的宽，字段容易在后续重构中漂移 | `frontend/src/components/vendor/RefundModal.vue`、`frontend/src/utils/refund.js`（只用 `order_line_id`/`remaining_qty`/`remaining_paid` 等） |
| society | `DELETE /societies/{id}` | `200` + `{"message":"社团已删除"}` | 改成 `204 No Content` | 删除成功没有可返回的信息；同仓里结算侧的删除（垫付 / 调整）已经是 204，此处是特例 | `frontend/src/stores/societyStore.js` 的 `deleteSociety` 完全不读响应 body，改 204 不影响它 |
| society | `PUT /societies/{id}`，body `{"is_home": false}` | `400` + `{"error":"不能取消本社团标记，只能把另一个社团设为本社团"}` | 改成 `409 Conflict` | 请求本身合法，是「世界的状态不允许」型冲突（与 `create_society` 重名 409 同类），现在的 400 语义偏「请求不合法」 | `AdminSocieties.vue` 只在设本社团时传 `is_home:true`，不会主动发 `false`；`societyStore.updateSociety` 只透传错误文字 |
| society | `POST /societies` | 请求体只声明 `name`，多余的键（如 `is_home`）被 serde 静默忽略 | 视契约决定：加 `deny_unknown_fields` 拒绝，或显式接受 `is_home` | 静默忽略会让「POST 里带 is_home」看起来生效其实没生效 | `societyStore.createSociety` 只发 `{ name }` |
| product | `POST /events/{event_id}/products` | 未传 `unit_price` 时用 `master_products.default_price`（REAL，**元**）`* 100.0).round()` 换算成分；`default_price` 不是任何请求/响应字段，故没有可标 `Money` 的 schema 位置 | 长远看可把 `default_price` 也迁到整数分；但代码注释说明 `.boothpack` 跨版本兼容靠它，不在本轮动 | f64 元与后端通用的整数分不一致，四舍五入是这里唯一把元变分的地方 | `AdminEventProducts.vue`（留空价即用默认价） |
| product | 多条（`add` / `update` / `restock` / `delete` + 错误体） | 错误文案中英混用：`Event not found`、`Product code not found in master catalog`、`Product not found`、`Product already in this event` 是英文；`商品不存在`、`单价不能为负`、`这个商品已经有进出记录，不能删除` 是中文 | 统一成中文（形状 `{"error": string}` 不变，只是文案） | 前端把 `err.response?.data?.error` 直接弹给用户，英文文案会出现在中文界面 | `eventDetailStore.js`（`addProductToEvent` / `updateEventProduct` / `restockEventProduct` / `deleteEventProduct` 的 catch）、`AdminEventProducts.vue` |
| product | `DELETE /products/{id}` | 成功体是 `{"message": "Product removed from event"}`，且返回 200 而非 204 | 可考虑改 204 或中文 message；但前端完全忽略 body，且改状态码属于契约变更 | 与其余接口的 204/中文风格不一致 | `eventDetailStore.js::deleteEventProduct`（只 `await`，不读 body） |
| auth | `POST /auth/login` | `DebugJson` 解析失败返回 400 **纯文本** `JSON Parse Error: …`（`text/plain`），不是 `{"error":…}` | 统一成 400 `{"error":"…"}`（`ApiError::BadRequest`） | 错误体收口；`services/api.js` adapter 与全仓到处读 `err.response.data.error` | `authStore.login` 只读 `.error`；该分支通常只在请求体被前端写坏时出现，已用形状测试钉住，**本次不改** |
| auth | `POST /auth/login` | `build_success_response` 里 `HeaderValue::from_str` 失败会返回 500 **纯文本** `"Invalid cookie value"` | 改成 `{"error":"…"}` | 同上，错误体收口 | 无（JWT 是 base64url，实际不可达） |
| auth | `POST /auth/login` | 登录失败一律 `AuthError::WrongCredentials` → 401 `{"error":"密码错误"}`；即使 `eventId` 指向不存在的展会也走同一分支（源码注释却写「返回 404」） | 展会不存在时返回 404 `{"error":"展会不存在"}`，只有密码错才 401 | 注释与实现不符，语义也更准确 | `authStore.login` 读 `.error`；`LoginView` 直接展示。属行为变更，**未动手** |
| auth | `POST /auth/login` | 三处密码查询（admin/vendor 全局、展会专属）与展会存在性查询都用 `.unwrap_or(None)` 吞掉 DB 错误，最终表现为 401 | 保留（登录失败不应因读设置失败而变 500/泄露 DB 错误），或改成 500 `数据库错误` | 记录「原来吞错误的继续吞」 | `authStore.login` |
| auth | `GET /auth/is-default-admin-password` | 读 `settings` 的 `fetch_optional(...).unwrap_or(None)` 吞掉 DB 错误，返回 200 `{"is_default": false}` | 保留（只是登录页提示，读失败不该阻断登录） | 记录吞错误 | `LoginView`（`!!data?.is_default`） |
| stats | `GET /events/{event_id}/stats`、`GET /events/{event_id}/sales_summary` | 403 是纯文本 `Access denied`、404 是纯文本 `Event not found`（`(StatusCode, &'static str)`），不是 `{"error": ...}` | 改用 `ApiError::Forbidden` / `ApiError::NotFound("Event not found")`（可让 403 文案与 guard 的「权限不足」统一） | 前端 client 统一读 `err.response?.data?.error`；纯文本会让错误提示变 `undefined` | `stores/eventStatStore.js`（404 只按 `err.response.status === 404` 处理，未读 error 字段）、`views/AdminEventStat.vue` |
| stats | `GET /events/{event_id}/sales_summary/download` | 403 纯文本 `Access denied`；读/生成失败是纯文本 500 `Failed to read excel` / `Failed to generate excel` | 换 `ApiError`（500 需要新变体或复用 `Db` 语义） | 同上；下载失败目前无任何可解析的错误体 | `views/AdminEventStat.vue` 的下载按钮 |
| stats | `GET /events/{event_id}/sales_summary/download` | 对**不存在的展会**不返 404：`unwrap_or(None)` 后回落到文件名 `Event {id}`，照样生成/下载一张全空的 xlsx | 先查展会存在性，不存在返 404（与同前缀另两条 GET 一致） | 三个接口对同一个不存在的 id 行为不一致，前端无法区分「空数据」与「id 写错」 | `stores/eventStatStore.js` 的 `downloadUrl`（直接拼 URL 交给浏览器/Tauri 下载） |
| stats | `GET /events/{event_id}/stats` | `events` 查询 `unwrap_or(None)`（DB 错误被当成 404）；汇总 `fetch_one(...).unwrap_or(SummaryStats{0,0,0})`；明细 `fetch_all(...).unwrap_or_default()` | 用 `?` 传播成 500 | schema 漂移或 DB 故障时用户看到「销售额 0 / 空表」而不是错误 | `views/AdminEventStat.vue`、`stores/eventStatStore.js` |
| stats | `GET /events/{event_id}/sales_summary` | `events` `unwrap_or(None)`；动态明细 `unwrap_or_default()`；总额 `unwrap_or((0,))`；时间序列 `unwrap_or_default()` | 同上 | 同上（趋势图静默变空线） | `views/AdminEventStat.vue`、`components/stats/SalesLineChart.vue` |
| stats | `GET /events/{event_id}/sales_summary/download` | `events` `unwrap_or(None)`；`details` `unwrap_or_default()`；所有 `worksheet.*` / `fs::remove_file` 写入错误用 `let _ =` 吞掉 | 至少记日志；明细查询用 `?` | 导出可能悄悄缺行/缺样式，且没有任何提示 | `views/AdminEventStat.vue` |
| stats | `GET /events/{event_id}/sales_summary` | `interval_minutes` 只认 30，其余（含非法值/负数/0）一律静默按 60 处理 | 显式校验并 400，或写明回退规则 | 客户端拼错粒度时口径被静默改掉 | `components/stats/StatFilters.vue`（只发 30/60） |
| master_product | `POST /{id}`、`POST/PUT /{id}/images/{image_id}`、`DELETE /{id}/images/{image_id}` | 404 是纯文本 `"Product not found"` / `"Image not found"` | 统一成 `{"error": "…"}`（`ApiError::NotFound`） | 前端只认 `err.response?.data?.error`，纯文本会走兜底文案，后端原因丢失 | `productStore.js`、`services/vision.js`、`api/core.ts` |
| master_product | `POST /`、`POST/PUT /{id}`、`PUT /{id}/status`、`GET /{id}/images` | 500 是纯文本 `"Database Error"` / `"File upload failed"` | 统一成 `{"error": …}`（`ApiError::Db` 等） | 同上；`core.ts` 的 `serverMessage` 读不到 | 同上 |
| master_product | `POST /{id}/images`、`POST/PUT /{id}/images/{image_id}`、`DELETE /{id}/images/{image_id}` | 部分 500 回显底层错误：`{"error": "Database Error: {e}"}` / `{"error": "{上传错误}"}` | 改成固定文案，不回显 `{e}` | 可能泄漏路径/SQL 细节，且与全仓「数据库错误」惯例不一致 | 同上 |
| master_product | `POST /`、`POST/PUT /{id}`、`PUT /{id}/status` | 成功响应 `MasterProduct.default_price` 是 `f64` **元**（`REAL` 列），全仓其余金额都是 `i64` 分 | 长期改成 `Money`（分）+ 前端换算，需要迁移 | 元/分混用是静默错账的高危源；本模块是唯一残留 | `CreateMasterProductForm.vue`、`EditMasterProductModal.vue`、`MasterProductList.vue` |
| master_product | `GET /` | DB 错误 `.fetch_all(...).unwrap_or_default()` ⇒ 200 空列表 | 返回 500，至少也落日志 | 前端把「加载失败」显示成「商品库为空」，用户无法察觉 | `productStore.js` |
| master_product | `POST/PUT /{id}` | `fetch_optional(...).await.unwrap_or(None)` ⇒ DB 错误被当成「商品不存在」404 | 区分「没有这一行」和「查询失败」 | 错误语义被吞，排查困难 | `productStore.js` |
| master_product | `GET /{id}/images` | `has_embedding_for_image(...).unwrap_or(false)` ⇒ 查询失败显示「未嵌入」 | 区分失败与 `false` | UI 把系统故障显示成正常状态 | `EditMasterProductModal.vue` |
| master_product | `POST /`、`POST/PUT /{id}` | `value.parse().unwrap_or(0.0 / default_price)` ⇒ 非法价格静默回落 | 返回 400 | 用户输入 `1,99` 会静默存成 0 | 前端表单 |
| master_product | `POST/PUT /{id}` | 上传失败仅 `eprintln!` 后继续，仍返回 200 | 上传失败应返回错误 | 用户以为图换了，实际没换 | `EditMasterProductModal.vue` |
| master_product | `PUT /{id}/status` | `Err(_)` 一律 500，既不打印也不区分 | 用 `ApiError::Db`（会自动落日志） | 排查困难 | `productStore.js` |
| master_product | 所有 multipart | `multipart.next_field().await.unwrap_or(None)`、`field.text().await.unwrap_or_default()` 吞解析错误 | 解析失败返回 400 | 截断的表单会被当成空字段 | 前端上传 |
| event | `GET /events` | DB 错误被 `.unwrap_or_default()` 吞成 200 `[]` | 让 DB 错误走 `ApiError::Db`（500 `{"error":"数据库错误"}`） | 数据库坏掉时前端静默显示「没有展会」，用户看到空列表 | `eventStore.fetchEvents`、`EventPortalView`、`VendorEventSelection`、`AdminLayout` |
| event | `GET /events/{id}` | DB 错误被 `.unwrap_or(None)` 吞成 404 `{"error":"Event not found"}` | DB 错误 → 500 | 把「库坏了」误报成「不存在」 | `eventStore`（列表/详情） |
| event | `POST /events` | 上传失败 500 `{"error":"Failed to save file"}`（JSON，非 `ApiError` 文案）；写库失败 500 为**纯文本** `Database Error` | 统一成 `{"error": ...}`（`ApiError::Db`） | 非 JSON / 非统一文案，前端 `err.response?.data?.error` 读不到 | `eventStore.createEvent`、`CreateEventForm.vue` |
| event | `POST`/`PUT /events/{id}` | 展会不存在 404 为**纯文本** `Event not found`；上传失败 500 纯文本 `File upload failed`；写库失败 500 纯文本 `Database Error` | 统一成 JSON `{"error": ...}` | 前端错误提示全失效（读了 `data.error`） | `eventStore.updateEvent`、`EditEventForm.vue` |
| event | `PUT /events/{id}/status` | 两处 DB 错误 500 为**纯文本** `Database Error` | 统一成 JSON（`ApiError::Db`） | 同上 | `eventStore.updateEventStatus`、`EventList.vue` |
| event | `DELETE /events/{id}` | 500 文案为 `Database error` / `Failed to delete event` / `Transaction commit failed`（JSON 但都非 `ApiError::Db` 的「数据库错误」） | 收口到 `ApiError::Db` / 具名错误 | 文案不统一，且把内部操作阶段泄给客户端 | `eventStore.deleteEvent`、`EventList.vue` |
| event | `POST`/`PUT`、`DELETE /events/{id}` | `.unwrap_or(None)` 吞查询错误；`let _ = delete_file(...)` 吞文件删除错误 | 至少把文件删除失败落日志 | 删码/删展会时物理文件静默残留（详见第 4 节） | `EditEventForm.vue`、`EventList.vue` |
| admin | `PUT /admin/password` | 500 分支 `"Admin password not set in database"` / `"Database Error"` 是 `text/plain` 裸字符串，不是 `{"error":…}` | 收口为 `ApiError::Db`（500 `{"error":"数据库错误"}`） | 前端一律读 `err.response?.data?.error`，纯文本分支拿到 `undefined`，只能落到兜底文案「修改失败」 | `frontend/src/views/AdminControlPanel.vue`（`e.response?.data?.error \|\| '修改失败'`） |
| admin | `PUT /admin/vendor-default-password` | 500 `"Database Error"` 纯文本，非 `{"error":…}` | 同上 | 同上 | `AdminControlPanel.vue` |
| admin | `PUT /admin/reset-database` | 500 混合：JSON `{"error":"无法重创建上传目录"}` / `{"error":"数据库重置事务提交失败"}`，以及纯文本 `"Database Error"` / `"Failed to clear data"` / `"Failed to reset settings"` / `"Failed to reset passwords"` | 纯文本分支统一成 `{"error":…}` | 同一路由的 500 有两种媒体类型/形状，前端无法统一解析 | `frontend/src/views/About.vue`（`e.response?.data?.error \|\| '重置失败'`） |
| admin | `PUT /admin/password` | 读 `settings` 用 `.unwrap_or(None)`，sqlx 错误被吞 | 改成 `?` 上抛 `ApiError::Db` | 数据库故障被伪装成「密码未设置」500，日志里也没有痕迹 | `AdminControlPanel.vue` |
| admin | `PUT /admin/reset-database` | 删除 uploads 目录失败时 `Err(_e) => {}` 直接忽略，继续清库并最终返回 200 | 明确返回错误，至少记 stderr | 图片没删干净但接口报「完全重置」，与文案不符 | `About.vue` |
| admin | `PUT /admin/reset-database` | `DELETE FROM sqlite_sequence` 失败用 `if let Err(_e) = … {}` 吞掉；各错误分支的 `let _ = tx.rollback().await` 也吞掉 rollback 失败 | rollback 失败至少记 stderr；自增 ID 重置失败记日志 | 事务回滚 / 自增 ID 重置失败被静默，排查时无线索 | `About.vue` |
| sync | `POST /sync/import-products` | 400 是纯文本 `"Upload error: {e}"` / `"No file found in request"` | 改成 `ApiErrorBody` 的 `{"error": "..."}`（400 `BadRequest`） | 全仓错误契约是 `{"error": ...}`；`extractImportError` 先读 `data.error`、再读 `data.message`，纯文本两条都读不到，只剩 axios 的通用 message | `frontend/src/stores/syncStore.js`（`extractImportError`） |
| sync | `POST /sync/import-products-raw` | 400 `"Invalid ZIP/Boothpack file"`、`"Failed to read catalog.json"`、`"Missing catalog.json in package"`、`"JSON Parse Error in catalog.json"`；500 `"DB Error: ..."`、`"DB Write Failed..."`、`"Commit Failed: ..."`、`"Internal error during import"` 都是纯文本 | 统一成 `{"error": ...}`；500 建议新增一个不泄露 sqlx 细节的 `ApiError` 变体（现有 `ApiError::Db` 文字固定「数据库错误」，不能承载这些文案） | 同上；且这些是导入失败时用户唯一能看到的诊断信息 | `syncStore.js`（`extractImportError`、`BoothpackSyncPanel.vue` 的 `syncError`） |
| sync | `GET /sync/export-products` | 500 是纯文本 `"Failed to load products"` 等 | 同上报错统一 | 导出失败时 `syncStore.js` 只把 axios 的通用 message 给用户，服务端文案被丢 | `syncStore.js`（`exportProducts` 的 catch） |
| sync | `POST /sync/import-products[-raw]` | 解压循环里 zip entry 读失败/`create_dir_all` 失败/`File::create` 失败/`io::copy` 失败、以及 `..` 路径穿越被拒，都只 `eprintln!` + `errored += 1` + `continue`，最终仍返回 200 `"Import successful"`，`errored` 不进响应 | 响应体加 `errored` 计数（或非零时给出警告），前端展示出来 | 部分图片静默丢失，用户以为导入完整 | `BoothpackSyncPanel.vue` 只读 `products_count`/`images_count` |
| sync | `GET /sync/export-products` | 单张图片 `std::fs::read` 失败或文件缺失时 `images_missing += 1`，仍返回 200 的 zip；`zip.start_file`/`zip.write_all` 失败也只 log 不中断 | 导出响应无法带正文（是 zip），建议至少在日志外把缺失清单回传/或在成功时也提示 | 「导出成功」的包可能缺图，且没有任何用户可见提示 | `syncStore.js`（只当二进制下载） |
| legacy | `GET /api/legacy/export.xlsx` | 失败响应是 `text/plain`：404 `没有可导出的历史数据`；500 `Failed to read excel` / `Failed to generate excel` | 统一成 `{"error": "..."}`，改用 `ApiError::NotFound` / 一个 500 变体 | 全仓错误体约定是 `{"error": "..."}`，这里是非标准出口；改成 JSON 会变字节，故本轮只记录 | `frontend/src/utils/legacyExport.js`（`.text()` 取前 200 字符拼进 `new Error`；`MigrationNotice.vue` / `AdminControlPanel.vue` 只把 `e.message` 弹窗） |
| legacy | `GET /api/legacy/export.xlsx` | 500 文案是英文 `Failed to read excel` / `Failed to generate excel` | 改成中文，和其它错误一致 | 用户可见文案全仓都是中文 | 同上一行 |
| legacy | `GET /api/legacy/export.xlsx` | 单元格写入 / sheet 名 / 列宽失败被吞：`let _ = worksheet.write_*`、`let _ = worksheet.set_name(...)`、`let _ = worksheet.set_column_width(...)` | 至少 `eprintln!` 或向上传播；失败时不要让用户拿到“成功但缺列”的 xlsx | 现在写单元格失败仍返回 200，导出内容静默不完整 | `legacyExport.js` 只判断 HTTP ok |
| legacy | `GET /api/legacy/export.xlsx` | 临时文件删除失败被吞：`let _ = fs::remove_file(&temp_file)` | 打日志 | 失败会在系统 temp 目录留下 `legacy_v1_export_*.xlsx`（含用户历史数据），静默泄漏 | 无 |
| legacy | `GET /api/legacy/export.xlsx` | 时间戳 `.unwrap_or(0)`、`Content-Disposition` 的 `HeaderValue::from_str(...).unwrap_or_else(|_| "attachment")` | 保留即可，属防御性兜底 | 两者在当前输入下都不会失败，改动无收益 | 无 |
| legacy | `GET /api/legacy/status`、`GET /api/legacy/export.xlsx` | `open_v1_backup_readonly` 的 `sqlx::Error` 被折叠成「没有备份」；`count_v1_rows` 用 `.ok()?` 把 SQL 错误折叠成「没有数据」 | 区分「文件不存在」和「文件在但打不开 / 读不出」，后者打 stderr | 损坏的 v1 库现在连日志都没有，排障时看不到 | `MigrationNotice.vue` / `AdminControlPanel.vue` 只消费 `has_backup` / 计数 |
| legacy | （无路由，仅内部） | `LegacyOrder.total_amount: f64`、`LegacyOrderItem.product_price: f64` 是**元**（v1 REAL），不是分 | 保持 `f64`，**不标** `#[schema(value_type = Money)]`；它们只用于写 xlsx，不进任何 JSON | 标成 Money 会把「元」误标成「分」；任务规定 f64 元的记入本节 | 无（`legacyExport.js` 拿到的是 xlsx 单元格，不是 JSON） |
| vision | `POST /vision/search` | 503 体有两种：`VISION_REBUILDING`/`VISION_NOT_READY` 是 `{"error":...,"reason":...}`，`VISION_BUSY` 只有 `{"error":...}` | 统一成 `{"error":...}`，或为 503 定义含可选 `reason` 的具名体并让三分支一致 | 同一状态码两种键集合；文档只能按 `ApiErrorBody` 记成单键 | `VisionSearch.vue` 只读 `err.response?.data?.error`；`services/vision.js` |
| vision | `POST /vision/models/install` | 400 有 `{"error":"unsupported source"}` 与 `{"ok":false,"error":msg}` 两种形状 | 统一为 `{"error": ...}` | `ok` 前端不读；同路由同状态码键集合不一致 | `services/vision.js` `installModel`、`VisionModelPanel.vue` |
| vision | `POST /vision/models/activate` | 400 `{"ok":false,"error":msg}` | 去掉 `ok` | 同上 | `services/vision.js` `activateModel`、`VisionModelPanel.vue` |
| vision | `DELETE /vision/models/{model_id}` | 400 `{"ok":false,"error":msg}` | 去掉 `ok` | 同上 | `VisionModelPanel.vue` `handleDelete` |
| vision | `POST /vision/feedback`、`PUT /vision/settings/ep` | 500 把内部错误原文回传：`{"error":"DB Error: {e}"}`、`{"error":"Failed to save: {e}"}`、`{"error":"Failed to load config: {e}"}` | 换成 `ApiError::Db`（固定「数据库错误」）或脱敏文案 | `crate::error` 的约定是 sqlx 原始错误绝不进响应体（可能带表名/列名/路径） | `VisionModelPanel.vue` 只读 `.error`；暴露面仍在 |
| vision | `PUT /vision/settings/ep` | 重新加载模型失败也返回 **200** + `ok:true` + 「配置已保存，但重新加载失败: …」 | 用 4xx/5xx + `ok:false`，或至少让前端知道未生效 | 前端把 `ok` 当成功提示（`actionMsgType='success'`），会误报「设备已切换」 | `VisionModelPanel.vue` `handleEpChange` |
| vision | `POST /vision/rebuild` | 成功 `message` 硬编码英文（`full/incremental rebuild started`），其它接口是中文 | 文案语言统一（可选） | 前端不读该字段 | `services/vision.js` `rebuildIndex`（忽略返回值） |
| vision | `POST /vision/search`（multipart 解析） | 吞错误：`multipart.next_field().await.unwrap_or(None)`、`field.text().await.unwrap_or_default()`、`top_k` `.parse().unwrap_or(5)`、`event_id` `.parse().ok()` | 区分「字段缺失」与「字段非法」，非法值返回 400 | 现在非法 `top_k` 静默变 5、解析失败可能与「image field is required」混淆；非 order/admin_event 模式下非法 `event_id` 被静默忽略 | `VisionSearch.vue`、`services/vision.js` |
| vision | `POST /vision/feedback`（multipart 解析） | 吞错误：同上；`chosen_master_product_id` `.parse().ok()`；`is_correct` 非白名单值静默为 false | 非法 id 返回 400 而非「required」；`is_correct` 非白名单报错或保持缺省 true | 错误提示会指错字段 | `VisionSearch.vue`、`services/vision.js` |
| vision | `GET /vision/models` | 无激活模型时 `active_model_id` 用 `unwrap_or_default()` 成 `""` | 保持，或显式 `null` 并在前端区分「未激活」 | 仅记录；前端用 `m.is_active` 找激活项，不直接读该字段 | `VisionModelPanel.vue` |
| vision | `GET /vision/settings/ep` | 运行时配置缺失时 `configured` 回落到 `"auto"`；`active` 可能是 `""` | 保持 | 仅记录 | `VisionModelPanel.vue` `loadEpSetting` |
